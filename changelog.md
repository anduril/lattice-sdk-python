# Changelog

## [4.20.0] - 2026-07-13
### Added
- **`RequestOptions.timeout`** — new `float` timeout field that takes precedence over the now-deprecated `timeout_in_seconds`, which remains supported for backward compatibility.

